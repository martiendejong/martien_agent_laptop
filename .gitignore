# Large binaries (download separately)
*.jar
*.exe
*.phar
*.dll

# Claude local settings (exclude settings, include skills)
.claude/settings.json
.claude/settings.local.json

# Node modules (reinstall with npm install)
node_modules/
playwright/node_modules/

# Compiled binaries
tools/cs-autofix/bin/
tools/cs-autofix/obj/

# Temporary/generated files
nul
*.log
*.tmp

# OS files
.DS_Store
Thumbs.db

# Editor files
*.swp
*.swo
*~

# Secrets (if any)
*.secret
*.key
secrets/

# OAuth credentials (contain client secrets)
_machine/gcp-oauth.keys.json
_machine/gdrive-credentials.json
_machine/gdrive.credentials.json
_machine/gdrive-sheets-credentials.json
_machine/client_secret_*.json
tools/gdrive-upload/credentials.json
temp_clickup.json
**/credentials.json
**/*.credentials.json

# Weird filename
C:*

# Machine temp files (created at runtime)
_machine/tmpclaude-*

# Bootstrap-generated (machine-specific state - templates tracked separately)
# These are created by bootstrap from .template files
09-SECRETS/
